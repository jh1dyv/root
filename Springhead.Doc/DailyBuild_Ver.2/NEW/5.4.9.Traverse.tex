% 5.4.9.Traverse.tex
%	Last update: 2018/06/05 F.Kanehori
%\newpage
\subsubsection{クラス Traverse}
\label{subsubsec:Traverse}

\bigskip
\begin{Brief}[b]
	指定されたディレクトリを起点としてディレクトリを走査し、
	テスト制御ファイルの内容に従ってビルド＆実行を制御する。
	テスト結果はイニシャライザに与えられた\tt{result}に設定される。
\end{Brief}

\begin{Initializer}{%
	obj = Traverse(testid, result, csc, control, section,\\
	\Hskip{70pt}toolset, platforms, configs, csusage, rebuild,\\
	\Hskip{70pt}timeout, report=True, audit=False,\\
	\Hskip{70pt}dry_run=False, verbose=0)}
	\Vskip{-.4\baselineskip}
	\begin{Args}[5em]
	    \Item[t]{testid}{%
		テスト種別(テスト結果ファイルの識別情報として使用する)。\\
		\tt{TESTID.STUB, TESTID.TESTS, TESTID.SAMPLES}のいずれか\Dagger}
	    \Item[t]{result}{%
		\tt{TestResult}クラスのオブジェクト。テスト結果をここに返す。}
	    \Item{csc}{%
	    	\tt{CloseSrcControl}クラスのオブジェクト。
		非公開ソースを使用するか否かを指定する。}
	    \Item{control}{%
		テスト制御ファイル名。\\
		タスクDailyBuildでは\Path{dailybuild.control}}
	    \Item{section}{%
		テスト制御ファイルのセクション名。\\
		Windowsのときは\tt{Windows}、unixのときは\tt{unix}を指定する。}
	    \Item{toolset}{%
		使用するCコンパイラのバージョン。
		WindowsのときはVisual Studioのバージョン、unixのときはダミー。}
	    \Item{platform}{%
		テストで使用するプラットフォーム。定数\tt{PLATS}のいずれか\Dagger}
	    \Item{configs}{%
		テストで使用するビルド構成。定数\tt{CONFS}のいずれか\Dagger}
	    \Item{cuusage}{%
		非公開ソースを使用するか否か。定数\tt{CSU}のいずれか\Dagger}
	    \Item{rebuild}{%
		強制的に再コンパイルするか否か(論理値で指定)。}
	    \Item{timeout}{%
		テスト実行時のタイムアウト値(秒で指定)。
		タイムアウトさせないときは値$0$を指定する。}
	    \Item{report}{%
		テスト実行状況レポート指定(論理値で指定)。}
	    \Item{dry_run}{%
		コマンドを表示するだけで実行はしない(論理値で指定)。}
	    \hline
	\end{Args}
	\Vskip{-.5\baselineskip}
	\small{\begin{Table}[t][15pt]{ll}
		\Item{\Hskip{15pt}\Dagger}{\Ref{subsubsec}{ConstDefs}}
	\end{Table}}
\end{Initializer}

\begin{Methods}{公開メソッド}
\Method{traverse(top)}{%
    \begin{Args}[r][5em]
	\Item{top}{走査開始ディレクトリ}
    \end{Args}
    \begin{Return}[r][5em]
	\MC{2}{l}{リターンコード}
    \end{Return}
    \begin{Brief}[r]
	引数で与えられたディレクトリを起点としてディレクトリツリーを走査し、
	対象となるすべてのディレクトリに対するテストを完了させる。
	個々のディレクトリの処理は\tt{process()}で行なう。
	テスト結果は引数で与えられた\tt{result}に設定される。
    \end{Brief}
    \begin{Proc}[r]
	\begin{enumerate}
	  \item	引数\tt{top}で指定されたディレクトリに移動する。
          \item	テスト制御ファイルを読む。
		次の条件がすべて成立したら\tt{process()}を呼び出して
		このディレクトリ自身の処理を行なう。
		\begin{itemize}
		  \item	このディレクトリが``対象外ディレクトリ''ではない。
			すなわち、
			\begin{itemize}
			  \item	ディレクトリ名が\tt{'.'}で始まらない。
			  \item	ディレクトリ名が\tt{Template}でも
			  	\tt{log}でもない。
			  \item	ディレクトリ名が\tt{toolset}で与えられた
				名前と等しくない。
			\end{itemize}
			(\tt{__is_candidate_dir()}で判定する)。
		  \item	テスト制御ファイルでExclude指定されていない。
		  \item	このディレクトリにsolution/makeファイルがある
			(\tt{__has_solution_file()}で判定する)。
		\end{itemize}
	  \item	テスト制御ファイルでDescendが指定されていたら、
		すべての``対象サブディレクトリ''に対して、
		\tt{traverse()}を再帰的に呼び出して処理を行なう。
		%\tt{\textasciicircum C}
		ただし途中で\Ctrl{C}割込があったら処理を中止する。
		%ただし途中で\verb|^C|割込があったら処理を中止する。
	\end{enumerate}
    \end{Proc}
}
\Method{process(cwd, ctl)}{%
    \begin{Args}[r][5em]
	\Item{cwd}{現在ディレクトリ}
	\Item{ctl}{\tt{ControlParams}クラスオブジェクト}
    \end{Args}
    \begin{Return}[r][5em]
	\MC{2}{l}{リターンコード}
    \end{Return}
    \begin{Brief}[r]
	カレントディレクトリ(のみ)に対するテストを実行する。
    \end{Brief}
    \begin{Proc}[r]
	\begin{enumerate}
          \item	solution/makeファイル名を決定する。
		デフォルトのファイル名は
		\Path{<\it{dierctory-name}><\it{compiler-version}>\tt{.sln}}%
		であるが、テスト制御ファイルのAlias指定により
		変更することができる
		(\tt{__solution_file_name()}で判定する)。
	  \item	イニシャライザの引数\tt{csc}およびテスト制御ファイルの
		UseClosedSrc指定に従って、非公開ソースの使用/未使用を設定する。
		(\tt{set_usage()}を呼ぶ。\Ref{subsubsec}{ClosedSrcControl})。
	  \item	\tt{BuildAndRun}クラスのオブジェクト\tt{bar}を準備する。
	  \item	指定されたプラットフォームとビルド構成のすべての組合せに
		対して、
		\begin{itemize}
		  \item	\tt{bar.build()}を呼び出してコンパイルを実行する。
	  	  \item	コンパイルが正常でかつ\Ctrl{C}割込がなかった場合に限り、
			\tt{bar.run()}を呼び出して生成されたバイナリを実行する。
		  \item	コンパイル結果および実行結果は\tt{result}に設定する
			(\tt{set_result()}を呼ぶ。\Ref{subsubsec}{TestResult})。
		\end{itemize}
	\end{enumerate}
    \end{Proc}
}
\if0
\bigskip
\noindent
\bf{内部メソッド \rm{(抜粋)}}

\vspace{5pt}
\def\COLWID{280pt}
\begin{enumerate}
  \item	\tt{__init_log(path, step, errlog)}\\
	概要
	\begin{narrow}[20pt]
		ログファイルをオープンしてヘッダ行を書き込む。
		ヘッダ行は次の3行からなる。
		\begin{itemize}
		  \item	日付行
		  \item	ログの識別行 --- 引数\ARG{step}および\ARG{erlog}を元に作成する
		  \item	空行
		\end{itemize}
	\end{narrow}
	\medskip

  \item	\tt{__chdir(path)}\\
	概要
	\begin{narrow}[20pt]
		\METHOD{os.chdir}のラッパ。指定されたディレクトリに移動する。
		移動先としてカレントディレクトリが指定されたときは何もしない。
	\end{narrow}
	\medskip

  \item	\tt{__is_candidate_dir(dir)}\\
	概要
	\begin{narrow}[20pt]
		指定されたディレクトリがテスト対象ディレクトリかどうか検査する。
		ディレクトリ名が次の何れかに該当するならば
		テスト対象外のディレクトリである
		(これらのディレクトリに\METHOD{traverse()}することを防ぐ)。
		\begin{itemize}
		  \item	ディレクトリ名が\TT{'.'}で始まる。
		  \item	ディレクトリ名が\TT{Template}または\TT{log}である。
		  \item	ディレクトリ名が\ARG{toolset}で与えられた名前と一致する。
		\end{itemize}
	\end{narrow}
	\medskip

  \item	\tt{__solution_file_name(ctl, dir, ccver)}\\
	概要
	\begin{narrow}[20pt]
		与えられた引数を元に、solution/makeファイル名を決定する。
		ファイル名はディレクトリ名(\tt{dir})と同じとするが、
		テスト制御パラメータ\VAR{CFK.SOLUTION_ALIAS}
		(\REF{subsubsec}{ConstDefs})で変更することができる。
		Windowsの場合には、末尾にVisual Studioバージョン(\ARG{ccver})および
		拡張子\TT{.sln}を付加する。
	\end{narrow}
	\medskip

  \item	\tt{__has_solution_file(ctl, dir, ccver)}\\
	概要
	\begin{narrow}[20pt]
		カレントディレクトリにソリューションファイルが存在するか否かを
		判定する。ファイルが存在しなければテストの対象外となる。
	\end{narrow}
	\medskip

  \item	\tt{__make_outpath(ctl, slnfile)}\\
	概要
	\begin{narrow}[20pt]
		出力バイナリのパス名を決定する。\\
		出力ディレクトリは、テスト制御パラメー\VAR{CFK.OUTPUT_DIR}が
		指定されていればそこに、指定がなければカレントディレクトリとする。\\
		ファイル名は、テスト制御パラメータ\VAR{CFK.BINARY_OUT}が
		指定されていればそれに、指定がなければソリューションファイル名と
		同じ(拡張子\TT{.sln}は除く)とする。

		\vspace{5pt}
		\footnotesize{%
		※ テスト制御パラメータ\VAR{CFK.OUTPUT_DIR}に指定されたマクロは、
		ここで次の値に変換される。\\
		\begin{tabular}{ll}
		    \MACRO{TOOLSET}
			& イニシャライザの引数\ARG{toolset}の値\\
		    \MACRO{PLATFORM}
			& \METHOD{process()}で設定された変数\VAR{self.platform}の値\\
		    \MACRO{CONFIGURATION}
			& \METHOD{process()}で設定された変数\VAR{self.config}の値
		\end{tabular}}
	\end{narrow}
	\medskip

  \item	\tt{__runtime_addpath(ctl, platform)}\\
	概要
	\begin{narrow}[20pt]
		次のパスリストを返す。これらは、 バイナリ実行時に加えるべきパスである。
		\begin{itemize}
		  \item	テスト制御パラメータ\VAR{CFK.ADD_PATH}に指定されている値。
		  \item	Windowsの場合、
			\PATH{.../dependency/bin/Win64;.../dependency/bin/Win32}
			または
			\PATH{.../dependency/bin/Win32}
			(プラットフォーム\ARG{platform}に従う）。
		\end{itemize}
	\end{narrow}
	\medskip

  \item	\tt{__report(msg_l, msg_s, header, newline)}\\
	\tt{__report_1(msg, header, newline)}\\
	概要
	\begin{narrow}[20pt]
		テストの経過報告を標準出力にレポートする。
		レポート出力の本体は\METHOD{__report()}であり、
		その形式は次のとおり。
		\begin{narrow}\relax
			[\IT{dir-name}\tt{:} ]<\IT{msg}>[\IT{nl}]
		\end{narrow}
		\IT{dir-name}と\IT{nl}の出力は、それぞれ引数\ARG{header}、
		\ARG{newline}にTrueが指定された場合のみ出力される。
	\end{narrow}
	\medskip

  \item	\tt{__status_str(step, status)}\\
	概要
	\begin{narrow}[20pt]
		引数\ARG{status}(数値)に対応した適切な文字列を返す。
	\end{narrow}

\end{enumerate}
\fi
\end{Methods}

% end: 5.4.9.Traverse.tex
