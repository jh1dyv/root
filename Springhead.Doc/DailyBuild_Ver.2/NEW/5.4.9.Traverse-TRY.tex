% 5.4.9.Traverse.tex
%	Last update: 2018/06/05 F.Kanehori
%\newpage
\subsubsection{クラス Traverse}
\label{subsubsec:Traverse}

\begin{ClassDesc}
指定されたディレクトリを起点としてディレクトリを走査し、
テスト制御ファイルの内容に従ってビルド＆実行を制御する。
テスト結果はイニシャライザに与えられた\ARG{result}に設定される。
\end{ClassDesc}

\begin{Initializer}{\tt{%
    \begin{tabular}{p{60pt}l}
	\MC{2}{l}{obj = Traverse(testid, result, csc, control, section,}\\
	& toolset, platforms, configs, csusage, rebuild,\\
	& timeout, report, audit, dry_run, verbose)
    \end{tabular}}}
\begin{Params}
    \Param{testid}{%
	テスト種別(テスト結果ファイルの識別情報として使用する)。\\
	\tt{TESTID.STUB, TESTID.TESTS, TESTID.SAMPLES}のいずれか\NOTE}
    \Param{result}{%
	\tt{TestResult}クラスのオブジェクト。テスト結果をここに返す。}
    \Param{csc}{%
    	\tt{CloseSrcControl}クラスのオブジェクト。
	非公開ソースを使用するか否かを指定する。}
    \Param{control}{%
	テスト制御ファイル名。\\
	タスクDailyBuildでは\FILE{dailybuild.control}}
    \Param{section}{%
	テスト制御ファイルのセクション名。\\
	Windowsのときは\tt{Windows}、unixのときは\tt{unix}を指定する。}
    \Param{toolset}{%
	使用するCコンパイラのバージョン。\\
	WindowsのときはVisual Studioのバージョン、unixのときはダミー。}
    \Param{platform}{%
	テストで使用するプラットフォーム。定数\tt{PLATS}のいずれか\NOTE}
    \Param{configs}{%
	テストで使用するビルド構成。定数\tt{CONFS}のいずれか\NOTE}
    \Param{cuusage}{%
	非公開ソースを使用するか否か。定数\tt{CSU}のいずれか\NOTE}
    \Param{rebuild}{%
    	強制的に再コンパイルするか否か(論理値で指定)。}
    \Param{timeout}{%
	テスト実行時のタイムアウト値(秒で指定)。
	タイムアウトさせないときは値\tt{0}を指定する。}
    \Param{report}{%
	テスト実行状況レポート指定(論理値で指定)。}
    \Param{dry_run}{%
	コマンドを表示するだけで実行はしない(論理値で指定)。}\hline
    \MC{2}{l}{\NOTE\hspace{5pt}\REF{subsubsec}{ConstDefs}}
\end{Params}
\end{Initializer}

\begin{Methods}{公開メソッド}
\Method{traverse(top)}{%
    \begin{Params}
    	\Param{top}{走査開始ディレクトリ}
	\Param{戻り値}{リターンコード}
    \end{Params}
    \begin{General}
	引数で与えられたディレクトリを起点としてディレクトリツリーを走査し、
	対象となるすべてのディレクトリに対するテストを完了させる。
	個々のディレクトリの処理は\tt{process()}で行なう。
	テスト結果は引数で与えられた\tt{result}に設定される。
    \end{General}
    \begin{Process}
	\begin{enumerate}
	  \item	引数\tt{top}で指定されたディレクトリに移動する。

          \item	テスト制御ファイルを読む。
		次の条件がすべて成立したら\tt{process()}を呼び出して
		このディレクトリ自身の処理を行なう。
		\begin{itemize}
		  \item	このディレクトリが``対象外ディレクトリ''ではない。
			すなわち、
			\begin{itemize}
			  \item	ディレクトリ名が\tt{'.'}で始まらない。
			  \item	ディレクトリ名が\tt{Template}でも
			  	\tt{log}でもない。
			  \item	ディレクトリ名が\tt{toolset}で与えられた
				名前と等しくない。
			\end{itemize}
			(\tt{__is_candidate_dir()}で判定する)。
		  \item	テスト制御ファイルでExclude指定されていない。
		  \item	このディレクトリにsolution/makeファイルがある
			(\tt{__has_solution_file()}で判定する)。
		\end{itemize}

	  \item	テスト制御ファイルでDescendが指定されていたら、
		すべての``対象サブディレクトリ''に対して、
		\tt{traverse()}を再帰的に呼び出して処理を行なう。
		ただし途中で\verb|^C|割込があったら処理を中止する。
	\end{enumerate}
    \end{Process}
}
\Method{process(cwd, ctl)}{%
    \begin{Params}
	\Param{cwd}{現在ディレクトリ}
	\Param{戻り値}{リターンコード}
    \end{Params}
    \begin{General}
	カレントディレクトリ(のみ)に対するテストを実行する。
    \end{General}
    \begin{Process}
	\begin{enumerate}
          \item	solution/makeファイル名を決定する。
		デフォルトのファイル名は
		\Path{<\it{dierctory-name}><\it{compiler-version}>\tt{.sln}}
		であるが、
		テスト制御ファイルのAlias指定により変更することができる
		(\tt{__solution_file_name()}で判定する)。

	  \item	イニシャライザの引数\tt{csc}およびテスト制御ファイルの
		UseClosedSrc指定に従って、非公開ソースの使用/未使用を設定する。
		(\tt{set_usage()}を呼ぶ。\REF{subsubsec}{ClosedSrcControl})。

	  \item	\tt{BuildAndRun}クラスのオブジェクト\tt{bar}を準備する。

	  \item	指定されたプラットフォームとビルド構成のすべての組合せに対して、
		\begin{itemize}
		  \item	\tt{bar.build()}を呼び出してコンパイルを実行する。
	  	  \item	コンパイルが正常でかつ\,\verb|^C|\,割込が
			なかった場合に限り、
			\tt{bar.run()}を呼び出して生成されたバイナリを実行する。
		  \item	コンパイル結果および実行結果は\tt{result}に設定する
			(\tt{set_result()}を呼ぶ。
			\REF{subsubsec}{TestResult})。
		\end{itemize}
	\end{enumerate}
    \end{Process}
}
\end{Methods}

\begin{Methods}{内部メソッド \rm{(抜粋)}}
\Method{__init_log(path, step, errlog)}{%
    \begin{General}
	ログファイルをオープンしてヘッダ行を書き込む。
	ヘッダ行は次の3行からなる。
	\begin{itemize}
	  \item	日付行
	  \item	ログの識別行 --- 引数\tt{step}および\tt{erlog}を元に作成する
	  \item	空行
	\end{itemize}
    \end{General}
}
\Method{__chdir(path)}{%
    \begin{General}
	\tt{os.chdir}のラッパ。指定されたディレクトリに移動する。
	移動先としてカレントディレクトリが指定されたときは何もしない。
    \end{General}
}
\Method{__is_candidate_dir(dir)}{%
    \begin{General}
	指定されたディレクトリがテスト対象ディレクトリかどうか検査する。
	ディレクトリ名が次の何れかに該当するならば
	テスト対象外のディレクトリである
	(これらのディレクトリに\tt{traverse()}することを防ぐ)。
	\begin{itemize}
	  \item	ディレクトリ名が\tt{'.'}で始まる。
	  \item	ディレクトリ名が\tt{Template}または\TT{log}である。
	  \item	ディレクトリ名が\tt{toolset}で与えられた名前と一致する。
	\end{itemize}
    \end{General}
}
\Method{__solution_file_name(ctl, dir, ccver)}{%
    \begin{General}
	与えられた引数を元に、solution/makeファイル名を決定する。
	ファイル名はディレクトリ名(\tt{dir})と同じとするが、
	テスト制御パラメータ\tt{CFK.SOLUTION_ALIAS}
	(\REF{subsubsec}{ConstDefs})で変更することができる。
	Windowsの場合には、末尾にVisual Studioバージョン
	(\tt{ccver})および拡張子\tt{.sln}を付加する。
    \end{General}
}
\Method{__has_solution_file(ctl, dir, ccver)}{%
    \begin{General}
	カレントディレクトリにソリューションファイルが存在するか否かを判定する。
	ファイルが存在しなければテストの対象外となる。
    \end{General}
}
\Method{__make_outpath(ctl, slnfile)}{%
    \begin{General}
	出力バイナリのパス名を決定する。\\
	出力ディレクトリは、
	テスト制御パラメー\tt{CFK.OUTPUT_DIR}が指定されていればそこに、
	指定がなければカレントディレクトリとする。\\
	ファイル名は、
	テスト制御パラメータ\tt{CFK.BINARY_OUT}が指定されていればそれに、
	指定がなければ
	ソリューションファイル名と同じ(拡張子\tt{.sln}は除く)とする。

	\vspace{5pt}
	\footnotesize{%
	※ テスト制御パラメータ\VAR{CFK.OUTPUT_DIR}に指定されたマクロは、
	ここで次の値に変換される。\\
	\begin{tabular}{ll}
	    \tt{TOOLSET}
		& イニシャライザの引数\tt{toolset}の値\\
	    \tt{PLATFORM}
		& \tt{process()}で設定された変数\tt{self.platform}の値\\
	    \tt{CONFIGURATION}
		& \tt{process()}で設定された変数\tt{self.config}の値
	\end{tabular}}
    \end{General}
}
\Method{__runtime_addpath(ctl, platform)}{%
    \begin{General}
	次のパスリストを返す。これらは、 バイナリ実行時に加えるべきパスである。
	\begin{itemize}
	  \item	テスト制御パラメータ\tt{CFK.ADD_PATH}に指定されている値。
	  \item	Windowsの場合、
		\Path{.../dependency/bin/Win64;.../dependency/bin/Win32}
		または
		\Path{.../dependency/bin/Win32}
		(プラットフォーム\tt{platform}に従う）。
	\end{itemize}
    \end{General}
}
\Method{__report(msg_l, msg_s, header, newline)}{x}
\Method{__report_1(msg, header, newline)}{%
    \begin{General}
	テストの経過報告を標準出力にレポートする。
	レポート出力の本体は\tt{__report()}であり、その形式は次のとおり。
	\begin{narrow}\relax
		[\it{dir-name}\tt{:} ]<\it{msg}>[\it{nl}]
	\end{narrow}
	\it{dir-name}と\it{nl}の出力は、それぞれ引数\tt{header}、
	\tt{newline}にTrueが指定された場合のみ出力される。
    \end{General}
}
\Method{__status_str(step, status)}{%
    \begin{General}
	引数\tt{status}(数値)に対応した適切な文字列を返す。
    \end{General}
}
\end{Methods}

% end: 5.4.9.Traverse.tex
